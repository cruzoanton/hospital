<h1>Doctor list</h1>
<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.name + " " + doctor.surname, doctor %>
    </li>
  <% end %>
</ul>

<%= link_to '<button type="button">New Doctor</button>'.html_safe, new_doctor_path %>